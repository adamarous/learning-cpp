/*
 * It is not legal. The first statement finishes when 
 * outputting v1 because there's a semicolon after it. Thus,
 * the rest of the statements where the output operator is
 * used cannot work because there's one missing operand.
 * To fix it, get rid of the semicolons at the end of both
 * the first and second lines.
 */ 