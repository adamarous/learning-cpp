// The while loop that specify, at some point of their execution, the same
// parameters as the ones in the for header, would benefit of using a for loop
// instead. In the exercises previously done, the while loop would always be
// worse to use.